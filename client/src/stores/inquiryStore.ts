// import { makeObservable, observable } from "mobx"

export default class InquiryStore{
    id = 1

    constructor() {
        // makeObservable(this, {
        //     id: observable
        // })
    }
}